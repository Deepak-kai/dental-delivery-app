import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.dentaldeliveryapp',
  appName: 'Dental Delivery App',
  webDir: 'build'
};

export default config;
